env: prod
timezone: America/Los_Angeles
scheduling:
  weekly_main: "0 9 * * MON"
features:
  forecast_horizon_weeks: 4
  theta_days: 7
  lead_min_coverage: 0.60
  alpha_effective_woc:
    fba: 1.0
    transfer: 0.8
    fbm: 0.5
llm_parent:
  model: deepseek
  temperature: 0.1
  response_format: json_object
guardrails:
  budget_change_pct_gate: 0.20
paths:
  outputs_dir: s3://bucket/scpc_outputs/${ISO_WEEK}/
